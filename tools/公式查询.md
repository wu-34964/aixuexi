---
title: 公式查询与学习
layout: page
---

<script setup>
import { ref } from 'vue'
import FormulaSearch from '../.vitepress/components/FormulaSearch.vue'
import FormulaLearn from '../.vitepress/components/FormulaLearn.vue'

const activeTab = ref('search')
</script>

<div class="formula-tool">
  <!-- Tab 切换 -->
  <div class="tab-bar">
    <button 
      class="tab-btn" 
      :class="{ active: activeTab === 'search' }" 
      @click="activeTab = 'search'"
    >
      🔍 查询公式
    </button>
    <button 
      class="tab-btn" 
      :class="{ active: activeTab === 'learn' }" 
      @click="activeTab = 'learn'"
    >
      🧠 学习公式
    </button>
  </div>

  <div class="tab-content">
    <FormulaSearch v-if="activeTab === 'search'" />
    <FormulaLearn v-if="activeTab === 'learn'" />
  </div>
</div>

<style scoped>
.formula-tool {
  max-width: 1200px;
  margin: 0 auto;
  padding: 10px 0;
}

.tab-bar {
  display: flex;
  gap: 4px;
  background: var(--vp-c-bg-soft);
  border-radius: 14px;
  padding: 6px;
  border: 1px solid var(--vp-c-divider);
  margin-bottom: 20px;
}

.tab-btn {
  flex: 1;
  padding: 14px 24px;
  border: none;
  border-radius: 10px;
  font-size: 18px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
  color: var(--vp-c-text-2);
  background: transparent;
}

.tab-btn:hover {
  color: var(--vp-c-text-1);
  background: var(--vp-c-bg);
}

.tab-btn.active {
  background: var(--vp-c-brand-1);
  color: white;
  box-shadow: 0 4px 16px rgba(59, 130, 246, 0.3);
}

.tab-content {
  animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: translateY(0); }
}
</style>